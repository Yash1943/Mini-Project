<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <title>Sign-up</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: rgb(165 180 252);
      }

      .container {
        display: flex;
        flex-direction: row;
      }

      .form-container {
        background-color: rgb(46 16 101);
        padding: 60px;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        margin: 10px;
        color: white;
      }

      .form-container h1 {
        text-align: center;
        margin-bottom: 10px;
      }

      .form-container form {
        display: flex;
        flex-direction: column;
      }

      .form-container input {
        margin-bottom: 20px;
        padding: 10px;
        /* border: 10px solid #ae87b2; */
        border-radius: 4px;
      }

      .form-container button {
        padding: 10px;
        background-color: rgb(163, 4, 255);
        color: #1a1212;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: medium;
      }

      .form-container button:hover {
        background-color: #0056b3;
      }
      .a {
        color: white;
      }
    </style>
  </head>
  <body>
    <div class="form-container">
      <h1 class="font-extrabold text-3xl m-3 p-4 py-1">Welcome</h1>
      <h2 class="font-normal text-2xl">MiniPay Sign Up</h2>
      <br />
      <form id="signupForm" action="/SignUpUser" method="post">
        <input type="hidden" name="_csrf" value="<%= csrfToken%>" />
        <input
          type="text"
          placeholder="Enter The Username"
          name="username"
          required
        />
        <input
          type="email"
          placeholder="Enter The Email"
          name="email"
          required
        />
        <input
          type="password"
          placeholder="Enter The Password"
          name="password"
          required
        />
        <input
          type="submit"
          class="button"
          value="Sign-up"
          style="background-color: rgb(96 165 250)"
        />
      </form>
    </div>
  </body>
</html>
